#!/usr/bin/env bash

# Starts the Rails web server only.
#
# Run `bin/reactive-view-dev` in a separate terminal for the ReactiveView daemon.

set -euo pipefail

exec bin/rails server -b 0.0.0.0 -p "${PORT:-3000}" "$@"

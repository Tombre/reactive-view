#!/usr/bin/env bash

# Wrapper script. Keep project-specific orchestration in bin/dev and delegate
# ReactiveView-managed startup behavior to bin/reactive-view-dev.

set -euo pipefail

exec bin/reactive-view-dev "$@"

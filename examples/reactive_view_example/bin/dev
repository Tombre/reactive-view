#!/usr/bin/env bash

# Starts only the Rails web process.
#
# Run `bin/reactive-view-dev` in a second terminal to start the daemon.

set -euo pipefail

exec bin/rails server -b 0.0.0.0 -p "${PORT:-3000}" "$@"

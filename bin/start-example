#!/usr/bin/env bash

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
APP_DIR="$REPO_ROOT/examples/reactive_view_example"

if [ ! -d "$APP_DIR" ]; then
  echo "Example app not found at $APP_DIR" >&2
  exit 1
fi

cd "$APP_DIR"
exec bin/start "$@"
